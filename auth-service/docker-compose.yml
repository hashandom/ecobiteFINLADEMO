version: "3.8"

services:
  mysql:
    image: mysql:8
    container_name: auth-mysql
    restart: always
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: auth_db
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: