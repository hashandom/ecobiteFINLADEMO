version: "3.8"

services:
  mysql:
    image: mysql:8
    container_name: batch_db
    restart: always
    ports:
      - "3309:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: batch_db
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: