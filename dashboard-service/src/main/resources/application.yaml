spring:
  application:
    name: dashboard-service

  config:
    import: optional:configserver:http://localhost:8888

  profiles:
    active: default

#server:
#  port: 8091
#
#spring:
#  application:
#    name: dashboard-service

